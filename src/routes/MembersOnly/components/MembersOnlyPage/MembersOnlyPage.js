import React from 'react'

export const MembersOnlyPage = ({ classes }) => (
  <div className={classes.container}>
    <span>MembersOnlyPage Component</span>
  </div>
)

export default MembersOnlyPage
